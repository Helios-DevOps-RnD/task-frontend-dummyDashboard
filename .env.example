# hms-fe — local dev env. Copy to .env.local and override as needed.
# These values are read by scripts/gen-env-config.js (predev hook)
# and written into public/env-config.js as window.__ENV__.
# ─────────────────────────────────────────────────────────────
API_URL=http://localhost:3001
CREATE_URL=http://localhost:3002
